package com.monopoly;
//import org.junit.Test;
//import static org.junit.Assert.assertEquals;

import com.monopoly.utilities.PlayerComparator;

import java.util.ArrayList;
import java.util.Collections;

public class Test {
    /*@Test

    public void testAdd() {
        String str = "Junit is working fine";
        assertEquals("Junit is working fine",str);
    }

    public void testUsernameSet() {
        for (Player player : Manager.getInstance().getPlayers()) {
            System.out.println(player.getUsername());
        }
    }
    */


/*
    public void sortingTestOfPlayers() {


        Player player = new Player("Bilgehan");
        Player player2 = new Player("Ahmet");
        Player player3 = new Player("Bilgehan");
        Player player4 = new Player("Ahmet");

        player.rollDice();
        player2.rollDice();
        player3.rollDice();
        player4.rollDice();

        ArrayList<Player> pl = new ArrayList<Player>();
        pl.add(player);
        pl.add(player2);
        pl.add(player3);
        pl.add(player4);

        Collections.sort(pl, new PlayerComparator());xw

        for (Player p : pl) {
            System.out.println(p.getDice()[0].getFaceValue() + "   " + p.getDice()[1].getFaceValue());
        }

    }
    */


}
